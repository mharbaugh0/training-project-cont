# Database container credentials (All required)
# The name of the database container in Docker
DATABASE_CONTAINER_NAME=

# The username for the database container (e.g., root_user)
DATABASE_CONTAINER_USERNAME=

# The password for the database container user (e.g., secure_password)
DATABASE_CONTAINER_PASSWORD=

# The name of the database to use (e.g., my_database)
DATABASE_NAME=

# MySQL database credentials (All required)
# The root password for MySQL (ensure it is secure)
MYSQL_ROOT_PASSWORD=

# The name of the MySQL database to create/use (ensure it matches DATABASE_NAME)
MYSQL_DATABASE=

# Prisma database connection URL (Required)
# Replace with the values you set in this example
PRISMA_DATABASE_CONNECTION_URL=mysql://${DATABASE_CONTAINER_USERNAME}:${MYSQL_ROOT_PASSWORD}@db:3306/${MYSQL_DATABASE}

# JSON Web Token secret (Required)
# A secret key used for generating JSON Web Tokens for authentication
JWT_SECRET=

# Note:
# 1. The DATABASE_CONTAINER_NAME should match the service name defined in docker-compose.yml.
# 2. The JSON Web Token (JWT_SECRET) is used for authentication and should be a secure, secret key.
# 3. Avoid using single-digit numbers for database names and credentials to prevent syntax errors.
#    Use meaningful, descriptive names instead.
# 4. Ensure DATABASE_CONTAINER_USERNAME, DATABASE_CONTAINER_PASSWORD, and MYSQL_DATABASE are correctly set
#    and match the corresponding values in your application configuration.
# 5. The DATABASE_CONTAINER_NAME is set to "db" to match the service name defined in your docker-compose.yml.
